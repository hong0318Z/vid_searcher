# 스트리밍 다운로더 의존성

# 필수
yt-dlp>=2024.1.0

# 선택 — Cloudflare 차단 사이트 impersonation 우회
# ⚠ yt-dlp 2026.03+ 기준: 0.5.10 ~ 0.8.x 만 지원. 0.9.x+ 는 API 변경으로 unsupported.
# curl_cffi>=0.5.10,<0.9.0
#
# Cloudflare 우회 권장 방법 (curl_cffi 없이도 동작):
#   1. 쿠키 파일: 브라우저 확장(Get cookies.txt LOCALLY)으로 내보낸 .txt 파일 사용
#   2. 브라우저 쿠키 자동 추출: firefox 또는 edge (Chrome 127+은 DPAPI 미지원)

# ffmpeg (시스템 패키지, pip 아님)
# 영상+오디오 자동 병합에 필요. 없으면 단일 스트림만 다운로드 가능.
# Windows : winget install ffmpeg
# Mac     : brew install ffmpeg
# Linux   : sudo apt install ffmpeg
