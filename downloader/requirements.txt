# 스트리밍 다운로더 의존성
yt-dlp>=2024.1.0

# yt-dlp가 내부적으로 ffmpeg을 사용합니다.
# ffmpeg 설치 (Windows): https://ffmpeg.org/download.html
#   또는 winget install ffmpeg
# ffmpeg 설치 (Mac): brew install ffmpeg
# ffmpeg 설치 (Linux): sudo apt install ffmpeg
#
# ffmpeg 없어도 단일 스트림 다운로드는 가능하지만
# 영상+음성 병합(bestvideo+bestaudio)은 ffmpeg 필요.

# Cloudflare 차단 우회가 필요한 사이트(예: FC2)는
# curl_cffi 도 설치 권장:
# pip install curl_cffi
