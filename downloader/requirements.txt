# 스트리밍 다운로더 의존성

# 필수
yt-dlp>=2024.1.0

# 권장 — Cloudflare 차단 사이트(FC2, 성인 사이트 등) 403 오류 해결
# ⚠ 버전 주의: 0.10 이상은 yt-dlp와 호환 안 됨 (unsupported로 표시되며 무시됨)
curl_cffi>=0.5.10,<0.10

# ffmpeg (시스템 패키지, pip 아님)
# 영상+오디오 자동 병합에 필요. 없으면 단일 스트림만 다운로드 가능.
# Windows : winget install ffmpeg
# Mac     : brew install ffmpeg
# Linux   : sudo apt install ffmpeg
